<UserControl x:Class="SharkSpirit.Editor.Views.Launcher.CreateProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <UserControl.Resources>
        <Style x:Key="Launcher.OpenProject.Button.Create" TargetType="{x:Type Button}" BasedOn="{StaticResource SharkSpirit.Button.Rounded}">
            <Setter Property="Content" Value="Create"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button Command="{Binding CreateCommand}" Style="{StaticResource Launcher.OpenProject.Button.Create}"/>
    </Grid>
</UserControl>
